`Class showing a while loop`
Object subclass: LoopTest
    category: examples(
        init (
            self makeLoop;
        )
        makeLoop |i n k| (
            i = 0;
            k = 20;
            "Loop Starting" println;
            k > 0 whileTrue: [i ++ ", " print; i = i + k; k = k - 1 ];
        )
)