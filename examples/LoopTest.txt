Object subclass: LoopTest
    category: examples
    (
        init (
            self makeLoop;
        )
        makeLoop |i| (
            i = 0;
            "Loop Starting" println;
            i < 20 whileTrue: [
                (i % 2) == 0 ifTrue: [
                    i ++ ", " print;
                ]
                else: ["ODD, " print];
                i = i + 1; 
            ];
        )
    )